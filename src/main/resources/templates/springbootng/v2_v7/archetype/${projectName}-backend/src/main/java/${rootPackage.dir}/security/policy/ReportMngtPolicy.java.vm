package ${rootPackage.name}.security.policy;

import org.keycloak.representations.idm.authorization.RolePolicyRepresentation;
import org.springframework.stereotype.Component;

import br.jus.tre_pa.jsecurity.base.policy.AbstractKcRolePolicy;

@Component
public class ReportMngtPolicy extends AbstractKcRolePolicy {

	@Override
	public void configure(RolePolicyRepresentation representation) {
		representation.setName("REPORT_MNGT_POLICY");
		representation.addClientRole(getKcProperties().getClientId(), "REPORT_MNGT", true);
	}

}
