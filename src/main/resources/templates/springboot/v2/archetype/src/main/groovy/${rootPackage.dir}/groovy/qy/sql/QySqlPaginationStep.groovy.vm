package ${rootPackage.name}.groovy.qy.sql;

import org.springframework.data.domain.Pageable;

interface QySqlPaginationStep extends QySqlFetchStep {
	QySqlPageableSelectStep limit(Pageable pageable);
}
