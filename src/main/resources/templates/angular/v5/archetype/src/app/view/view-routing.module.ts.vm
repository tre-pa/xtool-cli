import { ModuleWithProviders } from '@angular/core';
import { Routes, RouterModule } from '@angular/router';

const routes: Routes = [

];

export const viewRoutes: ModuleWithProviders = RouterModule.forChild(routes);